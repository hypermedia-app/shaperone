import { html, LitElement } from 'lit'

/**
 * Inherit to implement a `dash:MultiEditor` component rendered as Web Components using [lit](https://lit.dev/)
 */
export class MultiEditorBase extends LitElement {
  render() {
    return html``
  }
}
