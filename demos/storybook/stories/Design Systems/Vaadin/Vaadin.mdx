import { Markdown } from "@storybook/blocks"
import configure from './configure.ts?raw'

# Vaadin

Vaadin provides binding to use [Vaadin](https://vaadin.com/docs/latest/components) as form elements and form layout.

## Installation

```bash
npm install @hydrofoil/shaperone-wc-vaadin
```

## Setup

<Markdown>
  {`
  \`\`\`ts
${configure}
  \`\`\`
  `}
</Markdown>

